export function AlgeriaMapIcon(props: React.SVGProps<SVGSVGElement>) {
  return (
    <svg
      viewBox="0 0 800 600"
      xmlns="http://www.w3.org/2000/svg"
      stroke="hsl(var(--border))"
      fill="hsl(var(--card))"
      {...props}
    >
    </svg>
  );
}
